# methylation_coge-tools
Scripts to process bs-seq analyses to be used with CoGe (genomevolution.org).

To run coge-import_bismark or _pileometh keep the fix_chromosome_id.py module in the same directory.
